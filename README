Setup:
    npm install -g firebase-tools
    firebase login

The only files here should be publically available to download.

Run this command to update (regenerates files; 
commits to git; 
updates firebase)

    node bible_public_deploy.js